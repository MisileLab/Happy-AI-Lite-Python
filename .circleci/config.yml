version: 2.1
  
jobs:
  build:
    machine:
      image: ubuntu-2004:202010-01 # recommended linux image
    steps:
      - run: pip install --upgrade pip
      - run: pip install -r /bin/bash/requirements.txt
      - run: python Happy AI.py